%h1= t('operations.listing_models', :model => Equipment.model_name.human)

= paginate @equipments
- @equipments.each_with_index do |equipment, i|
  = render 'equipments/equipment', :equipment => equipment, :abstract => true, :number => i + 1
= paginate @equipments

%br

= link_to t('operations.new_model', :model => Equipment.model_name.human), new_equipment_path
