%h1
  = @peak.name
= render :partial => 'peaks/peak', :object => @peak, :locals => { :abstract => false }

= link_to t('operations.edit'), edit_peak_path(@peak)
\|
= link_to t('operations.back'), peaks_path
